import { Dimensions, StyleSheet } from 'react-native';

const deviceSize = Dimensions.get('window');

export default StyleSheet.create({
    categoryTitle: { color: "red", backgroundColor: "#e0e0e0" }
});